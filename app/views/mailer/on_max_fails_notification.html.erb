<h1><%=l(:rpp_body_max_login_failures_header)%></h1>
<p><%=l(:rpp_body_max_login_failures, timediff: Setting.plugin_redmine_account_policy[:user_timeout_in_minutes].to_s, date: DateTime.now.to_formatted_s(:long_ordinal), name: @user.name, login: @user.login)%></p>
